"""Framework-level reusable runtime primitives for Eylo.

Packages under ``eylo.framework`` are platform runtime building blocks, not
bounded domain modules. Domain modules may adapt these primitives, but the
framework package should not depend on product-specific module ownership.
"""
